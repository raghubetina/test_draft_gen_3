<div class="row justify-content-center mb-3">
  <div class="col-md-8">
    <div class="card">
      <h2 class="card-header h4">
        New Movie
      </h2>

      <div class="card-body">
        <%= render "form", movie: @movie %>

        <%= link_to fa_icon("chevron-left"), :back, class: "btn btn-block btn-outline-secondary" %>
      </div>
    </div>
  </div>
</div>
